# Persistent Docker Setup

Start the "persistent" container with the following command:

```bash
docker-compose -f docker-compose-persistent.yml up -d
```

This command will run the Docker container in detached mode, allowing it to run in the background.