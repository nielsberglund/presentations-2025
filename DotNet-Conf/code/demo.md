# Presentation / Demo

* Before presentation, spin up docker, create database with one table
* At slide 6/7 show the database.
* Slide 6/7 use Claude to see what tables are in the db
* At slide 8 "MCP Enterprise", show the MCP Server code in the DotNet folder
* Show tools
* Use ncpinspector to connect to the MCP server
    * npx @modelcontextprotocol/inspector --verbose

